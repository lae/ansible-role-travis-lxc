---
# defaults file for ansible-role-travis-lxc
template: debian
release: jessie
host_prefix: test
host_quantity: 1
container_config:
  - "lxc.aa_profile=unconfined"
  - "lxc.mount.auto = proc:rw sys:rw cgroup-full:rw"
