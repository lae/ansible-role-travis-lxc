---
# defaults file for lae.travis-lxc
test_profiles:
  - profile: debian-stretch
hosts_per_profile: 1
container_config:
  - "lxc.apparmor.profile = unconfined"
  - "lxc.mount.auto = proc:rw sys:rw cgroup-full:rw"
  - "lxc.cgroup.devices.allow = a *:* rmw"
additional_packages: []
